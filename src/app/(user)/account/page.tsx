interface AccountPageProps {
  searchParams?: Record<string, string | string[] | undefined>;
}

const AccountPage: React.FC<AccountPageProps> = ({searchParams}) => {
  return <div>Account</div>;
};

export default AccountPage;
